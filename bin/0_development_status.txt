Nuggit Development Status:

nuggit_checkout.pl
        - to do - add ability to checkout a specific file to undo changes.

nuggit_status.pl
    need to improve the output to not show files that have already been added to the index or differentiate it somehow?
    
nuggit_fetch.pl
nuggit_pull.pl

nuggit_diff.pl 
   to do - need to be able to diff between two hashes or two branches... going between two branches recursively may be tricky
   
???

nuggit_rm.pl - need to create this file, no placeholder currently exists

graph:
  git log --oneline --decorate --graph --all
